conky.config = {
    -- Window settings for i3 compatibility
    own_window = true,
    own_window_type = 'override', -- Try 'desktop' or 'override'
    own_window_transparent = true,
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',

    -- Positioning and size
    minimum_width = 200,
    minimum_height = 5,
    alignment = 'top_left', -- Changed from 'top_right' to 'top_left'
    gap_x = 10, -- horizontal gap from screen edge
    gap_y = 30, -- vertical gap from screen edge

    -- Other appearance settings (adjust as desired)
    double_buffer = true,
    update_interval = 0.99, -- update every 5 seconds
    draw_shades = false,
    draw_outline = false,
    draw_borders = false,
    text_buffer_size = 2048,
    override_utf8_locale = true,
    use_xft = true,
    xftalpha = 0.8,
    font = 'DejaVu Sans Mono:size=12', -- Choose your preferred font
    default_color = 'white', -- Choose your preferred text color

};

conky.text = [[
# ${color grey}Battery:${color} ${battery_percent BAT0}% ${battery_bar 10,50 BAT0} ${battery_time BAT0}
${color grey}RAM:${color} ${mem}/${memmax} (${memperc}%) ${membar 10,50}
# ${color grey}WiFi:${color} ${wireless_essid wlp2s0} (${wireless_link_qual_perc wlp2s0}%)

${color grey}Time:${color} ${time %H:%M:%S}
${color grey}Date:${color} ${time %B} ${time %d}
${color grey}Week of Year:${color} ${time %V}/52 ${color grey}${color}

The past is already gone,
the future is not yet here.
There's only one moment for
you to live, the present.
]];
